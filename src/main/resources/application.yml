spring:
  application:
    name: courses
  datasource:
    url: jdbc:postgresql://localhost:5432/courses_db
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
server:
  port: 8083
